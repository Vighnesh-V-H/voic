# Voic

Payment recovery infrastructure for Stripe merchants. Monorepo: `apps/frontend` (Next.js) + `apps/backend` (FastAPI). Start with `make dev`; see `docs/architecture.md` and `apps/backend/README.md`.
